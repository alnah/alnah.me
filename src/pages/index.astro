---
import NewsletterSignup from "../components/NewsletterSignup.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import PostList from "../components/PostList.astro";
import { getPublishedPosts } from "../lib/content";
import { buildWebsiteSchema } from "../lib/seo";

const posts = (await getPublishedPosts()).slice(0, 4);
---

<BaseLayout
  title="apprendre ou enseigner le français langue étrangère avec les technologies"
  schemaJson={buildWebsiteSchema()}
>
  <section class="home-shell content-band">
    <p class="eyebrow section-label">Articles récents</p>
    {
      posts.length > 0 ? (
        <PostList posts={posts} showReadingTime ctaHref="/articles/" ctaLabel="Parcourir tous les articles" />
      ) : (
        <p class="empty-copy">Aucun article publié pour le moment.</p>
      )
    }
    <NewsletterSignup />
  </section>
</BaseLayout>

<style>
  .home-shell {
    display: grid;
    gap: var(--space-5);
    padding-top: var(--space-8);
  }

  .section-label {
    margin: 0;
  }

  .empty-copy {
    margin: 0;
    max-width: var(--width-reading);
    color: var(--color-text-muted);
  }
</style>
